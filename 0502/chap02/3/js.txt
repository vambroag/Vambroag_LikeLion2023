let a = 5; # let allow to change the value, only need to be written once
const b = 2; # const = constant, if something is constant it doesn't change
let myName = "Ki Tae";
# by default start with const, only if you need to update your variable then use let like myName

console.log(a + b);
console.log(a * b);
console.log(a / b);
console.log("hello " + myName);
myName = "Vambroag";

console.log("your new name is " + myName);