const amIFat = null; # there is no value here, nothing exits here, different from false
# const amIFat = true;
# const amIFat = false;
# const amIFat = "true"; # this is string true, different from boolean true

let something; # just creating and didn't give a value, this is undefined different from null, exits inside the computer memory but doesn't have a value
# null doesn't happen automatically or naturally, null is used when we want to check if a something doesn't exist
console.log(something);